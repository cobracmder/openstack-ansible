---
- name: reboot server
  command: /sbin/reboot

- name: wait for reboot
  sudo: False
  delegate_to: localhost
  wait_for: 
    host: "{{ ansible_ssh_host }}" 
    port: "{{ ansible_ssh_port }}"
    delay: 30 
    timeout: 600
